{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch LLDB",
            "type": "lldb", // Changed from cppdbg
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "C/C++: g++ build active file" // Match your tasks.json name
        }
    ]
}
